# Development uses the Vite proxy by default.
VITE_API_PROXY_TARGET=http://127.0.0.1:3000

# Set this only when the deployed API is hosted at a different URL.
# VITE_API_URL=https://api.example.com/api
